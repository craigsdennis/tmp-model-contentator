{
  "name": "model-contentanator",
  "version": "1.0.0",
  "description": "Generates content files for models in docs based on the config",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "docs": "npx tsx ./scripts/gen-simple-docs.ts",
    "sanity": "npx tsx ./scripts/sanity-check.ts"
  },
  "author": "Craig Dennis <craig@cloudflare.com>",
  "license": "ISC",
  "dependencies": {
    "@cloudflare/ai": "^1.0.53",
    "dotenv": "^16.4.5",
    "json-to-pretty-yaml": "^1.2.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.19"
  }
}
